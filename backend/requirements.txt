# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# 数据库 / ORM
sqlalchemy==2.0.36
alembic==1.14.0

# 认证
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0

# 缓存 (可选, 未装 Redis 时自动降级内存缓存)
redis==5.2.1

# LangChain / RAG
langchain==0.3.14
langchain-community==0.3.14
langchain-ollama==0.2.2
langchain-chroma==0.1.4
chromadb==0.5.23

# 混合检索 (M7: BM25 关键词检索 + 中文分词)
jieba==0.42.1
rank-bm25==0.2.2

# 嵌入 / 重排 (优化阶段引入)
sentence-transformers==3.3.1
FlagEmbedding==1.3.3

# 文件解析
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5

# 评测 (M8)
# ragas==0.2.9
